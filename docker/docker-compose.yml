services:
  timescaledb:
    image: timescale/timescaledb:latest-pg16
    container_name: thoth_timescaledb
    restart: unless-stopped
    environment:
      POSTGRES_USER: thoth
      POSTGRES_PASSWORD: thoth_password
      POSTGRES_DB: thoth
    ports:
      - "5432:5432"
    volumes:
      - timescale_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U thoth" ]
      interval: 30s
      timeout: 10s
      retries: 5

volumes:
  timescale_data:
    driver: local
