services:
  timescaledb:
    image: timescale/timescaledb:latest-pg16
    container_name: fettle_timescaledb
    restart: unless-stopped
    environment:
      POSTGRES_USER: fettle
      POSTGRES_PASSWORD: fettle_password
      POSTGRES_DB: fettle
    ports:
      - "5432:5432"
    volumes:
      - timescale_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U fettle" ]
      interval: 30s
      timeout: 10s
      retries: 5

volumes:
  timescale_data:
    driver: local
